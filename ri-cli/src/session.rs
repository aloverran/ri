// Session management -- thin wrapper around ri-store's SessionFiling.
// Re-exports are available through ri::types::* (which re-exports ri_store types).
// This module is a placeholder for future CLI-level session commands.
