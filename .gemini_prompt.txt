I'm porting a TypeScript coding agent ("pi") to Rust. The original has 4 packages:
- pi-agent-core (1.5k LOC): Core agent loop with steering/follow-up queues, event stream, convertToLlm/transformContext hooks
- pi-ai (18.7k LOC): LLM provider abstraction (Anthropic, OpenAI, Google, Bedrock), streaming SSE, model registry, API key resolution  
- pi-coding-agent (36k LOC): Main CLI - session management (tree-structured JSONL), extension system (dynamic TS loading), tools (bash/read/write/edit/find/grep/ls), compaction, resource loading, 3 run modes (interactive/print/RPC)
- pi-tui (9.4k LOC): Terminal UI with differential rendering, component system, overlays, themes

Key architectural challenges for Rust:
1. Extension system: TS uses dynamic loading via jiti. Rust options: WASM plugins, dynamic libraries, Lua scripting, or just skip extensions initially?
2. Provider abstraction: TypeScript uses duck-typing + runtime registration. Rust needs traits.
3. Message type extensibility: TS uses declaration merging. Rust needs enums or trait objects.
4. Async streaming: TS uses async generators. Rust has async streams (futures::Stream).
5. TUI: ratatui vs crossterm-only vs custom?

Questions:
1. What Rust crates should form the foundation? (async runtime, HTTP, serialization, TUI, CLI parsing)
2. How should we handle the extension system? Skip it? Use WASM? Use trait objects with dynamic dispatch?
3. What's the right Cargo workspace layout?
4. Should the message types use enums (closed) or trait objects (open)? The original uses extensible union types.
5. What order should we implement the crates in?
6. Any pitfalls or anti-patterns to watch for in this kind of TS->Rust port?

Please be specific and architectural. This is for an expert Rust developer who values finished, total code over enterprise patterns.

---TASK---
Read .gemini_prompt.txt and complete the task after ---TASK---
